{
  "name": "@norma/core",
  "version": "0.1.0-pr1.0",
  "description": "Minimal TypeScript skeleton for the Norma Core engine boundary.",
  "type": "module",
  "private": true,
  "sideEffects": false,
  "packageManager": "pnpm@11.8.0",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "predemo:mvp": "pnpm run build",
    "demo:mvp": "node bin/norma-core-mvp-demo.mjs",
    "pretest": "pnpm run build",
    "test": "node --test tests/*.test.mjs",
    "test:orchestrator": "pnpm run build && node --test tests/orchestrator-v0.test.mjs",
    "check": "pnpm run test",
    "orchestrator:doctor": "pnpm run build && node dist/tools/orchestrator/src/cli.js doctor",
    "orchestrator:context": "pnpm run build && node dist/tools/orchestrator/src/cli.js context",
    "orchestrator:validate": "pnpm run build && node dist/tools/orchestrator/src/cli.js validate",
    "orchestrator:run": "pnpm run build && node dist/tools/orchestrator/src/cli.js run"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "typescript": "^5.8.0"
  }
}
